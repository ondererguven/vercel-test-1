"use client"

/**
 * This component is no longer needed since we're using middleware
 * for locale detection, but keeping it as a placeholder to prevent
 * import errors.
 */
export default function LocaleDetector() {
  return null
}
